(1) Suggested text: store the suggested text template that we can load. Example: on completing a checklist for ‘Plan transportation’
- Change to ‘Plan roundtrip transportation to <destination>’
- After clicking Plan button, suggested text would be ‘Flight: Home to <destination> <airline> (flight number)

(2) Popup: Destination, Itinerary, Budget edit button opens a popup while Checklist Open button opens at the side. There are two different behavior and both are good I can’t decide yet. Can we create a config from FE, open-as-popup as true all tabs behaviour similar to Destination, Itinerary, Budget, when false change them all to look similar as Checklist

(3) Checklist screen:
- Rename button ‘Save changes’ to ‘Save checklist changes’
- when a checklist is complete, hide ‘Plan’ or ‘Plan another’ button
- Support Bulk delete. Add checkbox beside each item of checklist and those selected can be deleted in one go. Still display the warning that others stay on itinerary for information

(4) Itinerary screen:
- when lodging is added from checklist, is should create itinerary on all the days it is active. ie check in is 24.12 check out at 27.12, should create lodging itinerary for 24, 25, 26, and 27
- Same with checklist, support bulk delete. Add checkbox beside each item. Remove individual delete button

(5) Budget screen:
- Show totals in: where do we get the list of currency here? It should only have currency of those that were added. ie, if user added expense in EUR and USD, only EUR and USD appears on the list. When a currency is selected there, is should convert the amount according to that currency converted from original currency.
- When adding expense, where do we get data info here? These currencies must be configurable. Check whether there is an api that returns list of currencies

(6) Published trip:
- there is no chart on Budget section
- On the published budget section, no need to display all budget description, only the chart and cost per category similar to template https://josephinealinea.dev/travel/2026-latam-trip/?show-only-budget=true