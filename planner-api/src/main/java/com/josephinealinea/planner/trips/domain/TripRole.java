package com.josephinealinea.planner.trips.domain;

/**
 * Members have equal rights over the trip's contents. The only things reserved
 * to the OWNER are deleting the trip and publishing it.
 */
public enum TripRole { OWNER, MEMBER }
